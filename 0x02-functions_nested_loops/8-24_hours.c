#include "main.h"

/**
 * jack_bauer - prints every minute of the day
 * Return: ...
*/

void jack_bauer(void)

{
	int min. hr;

	for (hr = 0; hr <= 23; hr++)
	{
		for (min = 0; min <= 59; min++)
		{
			_putchar(a + '0');
			_putchar(b + '0');
			_putchar(58);
			_putchar(c + '0');
			_putchar(d + '0');
			_putchar('\n');
		}
	}
}
